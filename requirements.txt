# TieOut runs entirely on the Python STANDARD LIBRARY — nothing to install.
# The CLI, the tests, the dashboard, and the AI layer all work out of the box.
#
# The AI layer talks to free LLM providers (Groq / Gemini / Ollama) over plain HTTP,
# so there is no SDK to install. Just set a key in .env (see .env.example) to switch it on.
