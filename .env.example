# Optional — the project runs fully WITHOUT these (rule-based fallback).
# Copy to .env and fill in to enable the real LLM for fuzzy adjudication + explanations.
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-3-5-haiku-20241022
